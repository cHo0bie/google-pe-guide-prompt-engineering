{
  "type": "object",
  "properties": {
    "customer_name": {
      "type": "string"
    },
    "card_last4": {
      "type": "string",
      "pattern": "^[0-9]{4}$"
    },
    "reason": {
      "type": "string",
      "enum": [
        "lost",
        "stolen",
        "expired",
        "damaged",
        "other"
      ]
    },
    "branch_or_delivery": {
      "type": "string"
    },
    "urgent": {
      "type": "boolean"
    }
  },
  "required": [
    "card_last4",
    "reason"
  ],
  "additionalProperties": false
}